interface:
  display_name: "Jev Replacement"
  short_description: "Assess, migrate, and evaluate Jev replacements"
  default_prompt: "Use $jev-replacement to assess this project, migrate the best candidate workflow to TypeSafe Jev, and evaluate it before and after."
policy:
  allow_implicit_invocation: true


interface:
  display_name: "Jev Replacement"
  short_description: "Evaluate and migrate bounded decisions to TypeSafe Jev"
  default_prompt: "Use $jev-replacement to evaluate this bounded semantic decision against TypeSafe Jev, choose keep, shadow-test, hybridize, or migrate, then benchmark the result."
policy:
  allow_implicit_invocation: true
